import { Link } from "gatsby"
import styled from "styled-components"

export const SocMedLink = styled(Link)`
  transition: 0.2s;
  padding: 0 0.25rem;
  :visited {
    color: #fff;
  }
  :hover {
    color: #fa9639;
  }
`
